/*
 * ab_ds_set.h — umbrella header for ab_ds set types
 * Part of ab_ds — https://github.com/UniquePython/ab_ds
 *
 * INCLUDES
 *   ss.h    — SortedSet       (sorted array + binary search)
 *   hs.h    — HashSet         (open addressing, linear probing)
 *   lhs.h   — LinkedHashSet   (HashSet + insertion-order iteration)
 *   ts.h    — TreeSet         (red-black tree)
 *
 * VERSION — 0.1.0
 */

#ifndef AB_DS_SET_H
#define AB_DS_SET_H

#include "hs.h"
#include "lhs.h"
#include "ss.h"
#include "ts.h"

#endif